# Uploads

This directory is used by the PHP backend for inspiration and uploaded track images.

The original development server contained sample image assets referenced by `database/MoodFlow_Database.sql`. They are not required to open or build the iOS project. When running a local backend, place the corresponding image files here or update the `imageUrl` values in the database.
